//
// useGameControls.js
//
// Skeleton custom hook for managing game controls and input
//
import { useEffect } from "react";

// PUBLIC_INTERFACE
function useGameControls() {
    /** Hook for controlling player input, binds keys & events. To be implemented. */
    useEffect(() => {
        // Placeholder
    }, []);
}

export default useGameControls;
